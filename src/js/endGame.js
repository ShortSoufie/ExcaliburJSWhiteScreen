import { Scene } from 'excalibur';
import { IntroScene } from './introGame.js';
import { MainScene } from './mainGame.js';

export class EndScene extends Scene {
    constructor(engine) {
        super(engine);
    }

    onInitialize() {
        // Add end scene initialization logic here
    }

    onActivate() {
        // Add logic to run when end scene becomes active
    }

    onDeactivate() {
        // Add logic to run when end scene becomes inactive
    }
}
